function createRandomEmployees(nEmployees, idDigits, minSalary, maxSalary, minYearOfBirth, maxYearOfBirth ){
let employees = [];
  minId = Math.pow(10, idDigits-1);
  maxId = Math.pow(10, idDigits);
array.forEach(nEmployees => {
  
});
}
